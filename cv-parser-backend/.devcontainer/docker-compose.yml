services:
  cv-parser-backend-dev:
    build: 
      context: ..
      dockerfile: .devcontainer/Dockerfile
      target: development
    container_name: cv-parser-backend-development
    ports:
      - "8000:8000"
      - "5678:5678"
    models:
      - llm
    volumes:
      - ..:/cv-parser-backend

  # cv-parser-backend-production:
  #   build:
  #     context: ..
  #     dockerfile: .devcontainer/Dockerfile
  #     target: production
  #   container_name: cv-parser-backend-production
  #   ports:
  #     - "80:8000"
  #   models:
  #     - llm    

models:
  llm:
    model: ai/gpt-oss:20B-UD-Q4_K_XL
